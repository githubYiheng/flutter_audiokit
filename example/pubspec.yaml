name: example
description: Example app for flutter_audiokit plugin.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.11.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_audiokit:
    path: ../packages/flutter_audiokit

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
